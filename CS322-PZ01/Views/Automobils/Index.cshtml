@model IEnumerable<CS322_PZ01.Models.Automobil>

@{
    ViewBag.Title = "Index";
}

<h2>Automobili</h2>

<p>
    @Html.ActionLink("Dodaj", "Create")
</p>
<table class="table table-striped table-hover">
    <tr class="info">
        <th>
            @Html.DisplayNameFor(model => model.AutoNaziv)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AutoNaziv)
        </td>
        <td>
            @Html.ActionLink("Uredi", "Edit", new { id=item.AutoID }) |
            @Html.ActionLink("Detaljnije", "Details", new { id=item.AutoID }) |
            @Html.ActionLink("Obriši", "Delete", new { id=item.AutoID })
        </td>
    </tr>
}

</table>
